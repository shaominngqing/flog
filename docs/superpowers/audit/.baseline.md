# Baseline snapshot — recorded at start of Phase 1

Date: 2026-04-22
Git HEAD at baseline recording: 5888264 (docs(superpowers): add Phase 1 implementation plan)
Phase 0 commit: f3b2a12

## Test state
- `cargo test`: 217 unit tests + 1 integration test, all green (recorded pre-Phase-0)

## Clippy state
- `cargo clippy --all-targets -- -D warnings`: FAILS
  - 1 error: approximate value of `f{32, 64}::consts::PI` found (in test code)
  - ~18 warnings (some duplicated across bin / test builds)
- Key warning categories observed:
  - dead_code: `MockRuleStore::enabled_count`, `LogStore::clear`, `adb::is_available`, `UsbDevice`, `list_devices`
  - missing Default: `App`, `LogStore`, `NetworkFilter`, `NetworkState`, `NetworkStore`
  - large size difference between enum variants
  - items after a test module
  - empty line after doc comment
  - manual char comparison / prefix stripping / redundant into_iter / same-type cast

## File size state (source lines)

Rust total: 18469 lines across `src/**/*.rs`.
Dart total: 1834 lines across `flog_dart/lib/**/*.dart`.

Red (> 800):
- src/event.rs                    1677
- src/ui/logs/mod.rs              1358
- src/app.rs                      1167
- src/ui/network/detail.rs        1109
- src/ui/source_select.rs          898

Yellow (500-800):
- src/ui/json_viewer/render.rs     745
- src/ui/network/mod.rs            700
- src/domain/structured_parser.rs  693
- src/transport/device_monitor.rs  654
- src/main.rs                      546
- flog_dart/lib/src/flog_dio.dart  504

## Documentation state
- Present: README.md, README_EN.md, CLAUDE.md, flog_dart/README.md, flog_dart/CHANGELOG.md
- Missing: ARCHITECTURE.md, MODULES.md, CONTRIBUTING.md, PROTOCOL.md

## Git state
- Branch: master
- Untracked outside docs/superpowers/: flog_062120.log (runtime output), flog_dart/test/ (scope TBD by user)
- No local uncommitted changes inside tracked files
